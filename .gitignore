out/
target/

# Render output. The CLI writes wherever `--out` points, and the demo
# invocations in docs/performance.md land these in the repo root.
frames/
*.mp4
*.mov
*.mkv
*.webm
*.avi
*.m4v
*.mxf
